apiVersion: v1
kind: Service

metadata:
  name: petclinic
  namespace: {{ .Release.Namespace }}

spec:
  selector:
    app: petclinic

  ports:
    - port: 80
      targetPort: 8080

  type: ClusterIP